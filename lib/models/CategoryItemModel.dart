import 'package:flutter/material.dart';

class CategoryItemModel{
  String name;
  Color categoryColor;
  String categoryImage;

  CategoryItemModel(this.name, this.categoryColor, this.categoryImage);
}